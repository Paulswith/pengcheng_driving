/*
  create at 2019/12/4 by 'itachy'
*/
use crate::pre_define::{api_define::*, const_define::account_info::*};
use std::collections::HashMap;


fn combine_login_uri(url: &str) -> String {
//    let params = BTreeMap::new();
    url.to_string()
}

fn combine_get_api_code_uri(url: &str) -> String {
    //TODO: insert param and param-value, bind to single uri return
//    let mut params = HashMap::new();
    url.to_string()
}
